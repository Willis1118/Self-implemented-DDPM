name: DDPM
dependencies:
  - pip
  - python >= 3.8
  - pytorch >= 1.13
  - torchvision
  - pytorch-cuda=11.7
  - numpy
  - pathlib
  - pip:
    - einops
    - datasets
    - matplotlib
    - tqdm
    - diffusers